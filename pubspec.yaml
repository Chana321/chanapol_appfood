name: chanapolapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_player: ^2.8.1
  geolocator: ^12.0.0
  geocoding: ^2.0.4
  flutter_map: ^8.2.2 
  latlong2: ^0.9.0
  url_launcher: ^6.2.5

  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/TomyumKung.mp4
    - assets/banoffi.mp4
    - assets/kaitod.mp4
    - assets/fast-food.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/fast-food.png"
